name: Custom Workflow
on: push
jobs:
  first-job:
    name: firstJob
    runs-on: ubuntu-latest
    steps:
      - name: Print Date
        run: date
      - name: Print Time
        run: |
          echo "Gaurav"
          echo "Chopra"
        
        
